export default class FavRestaurant {
  constructor(
    id,
    name,
    description,
    imageSrc,
    city,
    rating,
  ) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.imageSrc = imageSrc;
    this.city = city;
    this.rating = rating;
  }
}
